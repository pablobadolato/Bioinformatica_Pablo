print ("Hola")

#"" para que lo reconozca como texto

lista_numeros = c(10, 20, 30, 40, 50)

#numeros es el nomre de la lista

lista_numeros

#media es la variable resultado

media = mean(lista_numeros)
media
